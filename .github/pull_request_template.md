## Sync parity golden snapshots

If this PR adds, removes, or edits files under `apps/store-app/tests/sync-baseline/*.golden.json`:

- [ ] Golden diff is intentional; product/sync behavior change is acknowledged
- [ ] `pnpm run parity:gate` (from `apps/store-app`) passes locally
